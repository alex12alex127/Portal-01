<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <meta name="theme-color" content="#1e293b">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <title><%= typeof title !== 'undefined' ? title : 'Portal-01' %></title>
  <link rel="stylesheet" href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/css/style.css">
  <link rel="manifest" href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/manifest.json">
  <meta name="theme-color" content="#1a56db">
</head>
<body>
  <script>window.APP_BASE='<%= typeof basePath !== "undefined" ? basePath : "" %>';</script>
  <div class="app-wrap">
    <%- include('../partials/sidebar', { activePage: typeof activePage !== 'undefined' ? activePage : '' }) %>
    <main class="main">
      <div class="container">
        <%- include('../partials/breadcrumb') %>
        <%- body %>
      </div>
    </main>
  </div>
  <script src="<%= typeof basePath !== 'undefined' ? basePath : '' %>/js/app.js"></script>
  <script>if('serviceWorker' in navigator){navigator.serviceWorker.register('<%= typeof basePath !== "undefined" ? basePath : "" %>/sw.js').catch(function(){});}</script>
</body>
</html>
