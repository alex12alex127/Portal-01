<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Portal-01</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="auth-page">
  <div class="auth-box">
    <h1>Portal-01</h1>
    <h2>Login</h2>
    <form id="loginForm">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <input type="text" name="username" placeholder="Username" required>
      <input type="password" name="password" placeholder="Password" required>
      <button type="submit" class="btn">Accedi</button>
    </form>
    <p><a href="/auth/register">Registrati</a></p>
    <div id="msg"></div>
  </div>
  <script>
    document.getElementById('loginForm').onsubmit=async function(e){
      e.preventDefault();
      var btn=this.querySelector('button'),msg=document.getElementById('msg');
      msg.textContent='';btn.disabled=true;
      try{
        var r=await fetch('/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(this)))});
        var j=await r.json();
        if(j.success)location.href=j.redirect;else msg.textContent=j.error||'Errore';
      }catch(x){msg.textContent='Errore di connessione';}
      btn.disabled=false;
    };
  </script>
</body>
</html>
