<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registrazione - Portal-01</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="auth-page">
  <div class="auth-box">
    <h1>Portal-01</h1>
    <h2>Registrazione</h2>
    <form id="regForm">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <input type="text" name="username" placeholder="Username" required>
      <input type="email" name="email" placeholder="Email" required>
      <input type="text" name="full_name" placeholder="Nome completo" required>
      <input type="password" name="password" placeholder="Password" required>
      <button type="submit" class="btn">Registrati</button>
    </form>
    <p><a href="/auth/login">Hai già un account? Accedi</a></p>
    <div id="msg"></div>
  </div>
  <script>
    document.getElementById('regForm').onsubmit=async function(e){
      e.preventDefault();
      var btn=this.querySelector('button'),msg=document.getElementById('msg');
      msg.textContent='';btn.disabled=true;
      try{
        var r=await fetch('/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(this)))});
        var j=await r.json();
        if(j.success)location.href=j.redirect;else msg.textContent=j.error||'Errore';
      }catch(x){msg.textContent='Errore di connessione';}
      btn.disabled=false;
    };
  </script>
</body>
</html>
